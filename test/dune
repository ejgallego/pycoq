(rule
 (deps
  (:test-dir py)
  (package coq-serapi)
   ../build)
   (targets py-spec.log)
   (action
    (with-outputs-to %{targets}
     (setenv PYTHONPATH ../build/lib
      (run pytest %{test-dir})))))
